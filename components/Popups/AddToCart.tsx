import React from 'react'

type Props = {}

const AddToCart = ({}: Props) => {

  return (
    <div className={`rounded-md bg-black bg-opacity-40 py-2 px-4 mb-2 text-white`}>Товар добавлен в корзину</div>
  )
}

export default AddToCart